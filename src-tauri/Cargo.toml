[package]
name = "onepku"
version = "0.6.0"
edition = "2021"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["macos-private-api"] }
campus-core = { path = "../crates/campus-core" }
serde_json = "1"
rfd = "0.15"
