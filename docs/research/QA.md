# OnePKU 本地验收记录

验收日期：2026-09-09（北京时间）。范围为此 Mac 的本地应用，未发布或公证。v0.2 已按约定取消个人课程时间表，以下为本地验证记录。

## 真实路径

- 原生 Mac 应用启动、Tauri IPC：读取当前学期 8 门课程；今日作业详情显示真实 Lab 0 内容，来源无截止时间时不制造截止日期。
- 课程：搜索、展开资料、实际下载 PDF 成功，文件识别为 55 页 PDF。下载位置为用户 Downloads/OnePKU。
- 树洞：最新列表、ICS 搜索、正文及回复读取成功。没有发送帖子、评论或点赞。
- 校园卡：实际余额和流水可读取；收支方向按类型处理，日期与余额字段已修正，无 NaN。
- 公共资源：空闲教室查询及筛选、官方 PDF 校历，独立于登录资源。
- 登录：原生二维码显示与等待扫码状态正常，取消保留原会话。短信空值校验通过；未为验收额外发送短信。
- 保持登录：设置开关开/关及保存后恢复通过，当前开启。后台计划和认证失败停止策略通过单元测试；未做跨日持续运行验收，不能承诺永久登录。

## 个人课程时间表的范围决定

在已登录的 Chrome 官方页面检查了真实响应：

1. 树洞 `/api/getCoursetable_v2`：`code: 20000`、`success: true`，但 `data` 仅含 `flag: "false"`，没有 `course`。
2. 门户 `/publicQuery/ctrl/topic/myCourseTable/getCourseInfo.do?xndxq=26-27-1`：`success: true`，同时 `message: "获取个人课表信息失败"`，没有课程数组。
3. 官方门户前端仅检查 `success` 后访问 `courses.length`，因此页面呈空表；这不证明今天无课。

用户随后明确允许不提供个人课程时间表，本版已移除其页面、错误提示与星期栏。教学网课程和作业继续使用；校历独立展示官方整张 PDF。

## 隔离、访问及构建

- 7 项前端测试通过：独立资源错误及旧数据、流水方向、来源失败隔离、已读撤回与重挂载持久化、异常偏好恢复。
- 本轮 cargo test --workspace：16 项 Rust 测试通过，包括缓存原子写入与权限、过期及账号隔离、持久化白名单、通知来源解析与 URL 限制；上游补丁回归已在上一轮检查。
- 正式 Tauri app 构建、TypeScript、格式检查、设计 token 一致性通过。
- 开发预览只绑定 127.0.0.1；真实检查合法请求 200、外部 Origin 403、缺失自定义头 403、未知 shell 命令 400、路径穿越 403。正式桌面应用不启动预览 HTTP 服务。
- 浏览器无控制台错误；760 CSS px 窗口宽度无横向溢出，今日布局转为单列。
- Premium strict audit：0 findings。Design lint：0 errors；工具的 CSS 引用识别造成 13 个 orphan token 提醒，token 生成与使用已另外核对。

## 与概念图的对照

对照对象：[概念图](concept.png) 与 [新版首页](../onepku-preview.png)，另有 [Mac 通知阅读页](../onepku-mac.png)。

| 项目     | 实际结果与有意调整                                                                                   |
| -------- | ---------------------------------------------------------------------------------------------------- |
| 布局     | 保留左侧导航与主次双列，移除个人课表星期条；侧栏 204 px，更紧凑。窄屏单列。                          |
| 色彩     | 白色页面、浅灰侧栏、北大红选中状态、浅粉余额块，与概念一致。修正了侧栏宽度和颜色变量同名的问题。     |
| 字体     | 使用系统中文无衬线字体，替代概念中的装饰衬线标题，符合简洁日常使用要求。                             |
| 图标     | 统一 lucide 线性图标；省略概念里的塔影装饰，保留信息和操作层级。                                     |
| 文案     | 导航按学习、校园、生活分组，通知和校历成为独立页；去掉解释性口号；用真实数据、局部错误替换概念示例。 |
| 操作     | 各块单独刷新；登录、短信、搜索、详情、下载均有反馈。概念没有体现的错误/旧数据状态已补齐。            |
| 内容密度 | 根据真实课程、通知与作业数量显示，不为贴近概念而填入示例内容。                                       |

截图包含用户当前校园数据，仅用于本地查看，尚未对外发布。开发临时截图已清理，以上三张图作为用户可查看的设计和验收材料保留。

## v0.2 真实新增路径

- 读取五个来源共 71 条通知。学校、各单位、教务、信科均成功读取正文；教务附件的相对链接转换为官方完整 URL。各单位公告第二页加载后显示 40 条，搜索无结果状态通过。
- 浏览器刷新后已读状态和来源开关保留；原生应用完全退出重开后未读计数仍为 70，测试项已撤回未读恢复为 71。760px 宽度正文可完整阅读并返回列表。运行日志未捕获控制台错误。
- 本机原生 WebKit 中验证 2026–2027、2025–2026 官方整张 PDF、125% 缩放和适合宽度。修复 tauri 自定义来源的 module Worker 兼容问题，单页使用 PDF.js 主线程处理器。
- 重启后台与原生应用后，课程作业、余额、通知和 PDF 可从磁盘恢复；更新失败保留同账号旧内容由自动化测试覆盖。
- v0.2 app 构建、格式检查、token 校验及 strict audit（0 findings）通过。未对学校所有通知及附件逐条验收。

## v0.2.1 界面与能力盘点

- 校园卡使用用户提供的学校卡面作为设计参考，生成无个人信息的独立背景；首页与账户页共用组件，正常/冻结/挂失状态为实时文本。
- 移除斜向链接箭头、正常状态的重复校园卡标签；正常更新时间仅在刷新按钮悬停提示，实际检查 title 含上次成功时间，正文无常驻更新时间。
- 更新失败和旧数据提醒仍然可见。本轮真实校卡接口恰好返回“登录已失效”；修复其被归类为 unavailable 的问题，补入认证分类回归用例。
- 7 项前端、16 项 Rust 库测试通过，token/格式检查及 strict audit 无错误。原生构建通过，图片是本地打包资源。
- 功能盘点覆盖此前选择的参考项目，记录已接入、部分接入、候选与验证条件，详见 CAPABILITIES.md。没有把上游声明当作本机已接入。

最终安装核对：Info.plist 版本为 0.2.1，安装二进制与本轮构建 SHA-256 相同。原生设置页显示三服务最近验证成功，其中校园卡为 9/9 18:31；未据此推断用户具体采用何种重新登录操作。用户随后正在浏览原生应用，停止主动切页。

## v0.3.0 学习与统计增量（2026-09-09）

- 当前账号实际读取 28 门当前/历史课程；浏览器切换上学期仅显示该学期课程。当前课名中的括号保留。
- 可视计算与交互概论回放列表返回 9 月 7 日课程；原生有同一回放列表及教学网页播放入口。没有验证视频完整播放，也未实现视频离线下载。
- 正式成绩返回 19 条，浏览器学期筛选生效；缺失 GPA 显示破折号；不记录个人分数到项目证据。考试源不可用明确报错，成绩仍可用。
- 月度总额、每日及分类统计读取成功并相互对上；原生显示正常。月份切换、分类失败不影响汇总已覆盖前端测试。
- 单份真实 PDF 课件下载到学期/课程目录，重复下载没有生成副本；9,089,316 字节，SHA-256 与 sidecar 元数据一致。不同内容同名文件保留版本由 Rust 测试覆盖。
- 树洞已从导航和可达页面移除；底层 API 保留，未执行任何发帖/回复/提交作业/支付操作。
- 前端 10 项、Rust 19 项测试通过；资源账号变更清理覆盖历史课程、回放、成绩、考试、统计。原生 release 构建通过，安装版本 0.3.0，安装二进制 SHA-256 与构建产物一致。
- 界面沿用原概念的白底、灰侧栏、酒红选中态、系统字体与线性图标。按用户决定移除树洞入口，学习增加成绩与考试；卡面与月度概览并列，图表下一行，减少空白和滚动。弹窗使用既有 Radix 组件。严格设计检查、设计 token 检查通过。

重启验收：退出后重新启动 0.3.0，校园卡总额、每日与分类统计直接恢复，刷新提示仍为重启前 19:29，证明本轮新增统计资源走了持久缓存恢复。临时预览服务及测试浏览器页面已关闭，原生应用保持打开。

## v0.4.0 第一批写入口（2026-09-09）

- 吸收判据记录在 ../ADOPTION.md：GUI/原站/CLI-MCP/暂缓四类，逐项说明收益和验收。
- 前端 14 项、共享核心 24 项、course 库 3 项，共 41 项测试通过。包含选择/预览不提交、最终点击仅一次、关闭取消、选文件取消、教师无需提交提示、持久发送状态不可重发、过期发送进入待核对、回执文件名/内容匹配和损坏日志拒绝。
- 本地实际暂存验证通过：选择文件后更改源文件，暂存副本内容仍保持不变；取消后暂存文件删除。对当前 Lab 0 的后端预览请求被“无需提交文件”规则拦下，没有创建提交操作；未请求学校文件提交。原生文件选择器已编译接入，当前没有合适的真实待交作业来走完整真实提交旅程。
- 浏览器中作业说明与附件读取成功（使用 mode=view），Lab 0 显示教师无需提交。清除查看模式的空附件行。浏览器预览服务对 CommitSubmission 返回 HTTP 403，不允许通过预览服务写学校。
- 作业提交的成功判据是学校回执附件内容与暂存 SHA-256 匹配，不以 HTTP 状态单独确认。中断保持待核对，不自动重发；记录属于登录会话，保存在本地私有目录。
- 原生 release 构建通过，设计严格检查、token 与格式检查通过。生产环境的实际文件提交尚未执行，不能声称已经通过学校端提交验收。

安装核对：Info.plist 为 0.4.0，安装二进制 SHA-256 与 release 产物一致。原生 Lab 0 显示无需提交说明及两个有效附件；设置页显示操作记录（空）、原站办理与 0.4.0；校园卡页出现充值与支付入口。没有为验证界面添加学校提交记录。

## v0.5.0 当前验证（2026-09-09）

- 真实账号读取当前与历史课程、教学网分数源；独立作业页中 Lab 0 的说明、附件和无需提交状态正常。智云目录查询与全部课次成功；仅显示已有回放与分页减少大量尚未录制课次的干扰。
- 教学网实际视频分段下载约 49 MB；ffprobe 识别 H.264 1920x1080 与 AAC，15 秒样本合并为 MP4 成功；取消后无残留临时分段目录。尚未完成一整节课程的下载验收。新增“播放并下载”保持原站播放和后台归档并行。
- 北大空间真实扫码登录成功，读取二教 10 间教室。时段接口返回学校“请先完善个人信息”，需联系电话与邮箱；已改为专门恢复提示，无重新扫码要求、无整片未知时段表。原字段 locked=2 的意义未验证，移除错误的“已锁定”解释。尚未完成资料后的占用表验收，未提交或取消预约。
- 体育、图书馆预约为学校原站窗口，尚未接入原生时段 API。提醒默认关闭，5 分钟检查、首次静默基线、账号隔离和本地去重已实现；未启用个人系统推送验收，退出应用不运行。
- 贡献候选三项只读 CLI 测试通过，实际 CLI 按日期和名称查询返回智云课程；未推送、未创建 PR。全局 pku CLI 未改动。

最终补充：用户在原站尝试保存资料后，检查实际“申请教室”入口发现学校明确显示“未开放预约申请”。核心现先读学校开放状态，再决定是否查询时段；真实查询已返回 bookingClosed，取代资料补全提示。没有证据说明该系统已废弃，也没有声称当前可以预约。最终前端 19 项、核心 27 项、vendored course 7 项测试通过；贡献候选的 3 项测试单独记录。严格设计检查与格式、token 校验通过。

最终产品取舍：用户要求不展示不可用入口，场地预约主导航、路由和北大空间登录入口已撤下。预约相关源码与测试保留，暂不作为 0.5 的用户功能发布。

## 不可用入口清理

2026-09-09 再次真实调用考试接口，仍返回 noExams；其上游 getCoursetable_v2 返回不可用标志。撤下考试安排页签及后台自动请求，页面改名为“成绩”，旧页面链接仍转入成绩页。正式成绩、29 门当前与历史课程、作业、课程通知和校园卡均返回成功，未将正常空记录误判为源不可用。

## v0.6.0 回放与原文（2026-09-09）

- 真实教学网《计算机系统导论》2026-09-07 第 5–6 节返回 717 个分片、总长约 1:59:26；已在浏览器预览及 Mac 原生 WKWebView 显示 1920×1080 H.264/AAC 画面，原生播放时间持续推进。
- 从开头用进度条跳到约 1:00:55（尚未完整缓存处），随后继续播放到 1:01:42；退出并重启应用后恢复此前的 1:43，缓存被复用并继续增长。没有宣称完整观看或下载了整节两小时录像。
- 31 个 core 测试、8 个教学网库测试、21 个前端测试通过。新增测试覆盖本机范围请求、跨站拒绝、源不可用时继续读取缓存、关闭失效、无效视频片段、观看位置恢复、缓存控制及离开后的迟到请求清理。
- 内置原文窗口已在原生应用打开校内门户的真实统一认证页面；门户要求在网页窗口正常登录。公开图书馆活动索引读取 48 项，原生窗口已实际渲染 AI 辅助 3D 建模讲座的完整官网页面、海报、时间及地点；公共正文已改为点击原文直接打开窗口，不再在 GUI 请求清洗后的正文。
- 业务 IPC 仍只允许主窗口调用。媒体服务器仅绑定 127.0.0.1，使用不可猜的会话路径并限制 Origin，不向播放器暴露学校鉴权信息；原文窗口独立且无本地命令权限。
- 完整课程下载、长时间弱网、所有不同录制格式尚未逐一跑完。MP4 归档与本地播放器缓存目前独立；仅 TS/AES-128 的已完成回放支持本地播放。

本地 Mac 构建启用 Tauri 的 macos-private-api/fullscreen 功能，WKWebView 默认未开启 HTML 媒体全屏；此构建仍仅用于当前 Mac，不作为 App Store 包发布。

最终原生验收：播放器“全屏”按钮实际进入媒体全屏，Escape 返回课程弹窗且播放继续；随后关闭播放器，结束验收播放和后台缓存。安装包版本 0.6.0，安装二进制与本次 release 产物 SHA-256 一致（cacaad684b4e2013ddbeae1c074976925e6d056a42dceb3b0e8f4911349bb6a5）。

## v0.6 界面简化与主区域详情（2026-09-09 后续）

- 播放器收起重复日期、时长、说明和缓存管理；播放时隐藏回放列表，次要操作放入“更多”。进度条清除普通输入框的内边距，使用独立轨道/滑块样式，真实预览拖动到约 59 分钟后画面、时间和填充同步。
- 课程详情改为右侧主区域页面，默认回放，可切换资料与教学网成绩；课程中移除作业入口。独立智云课堂实录 GUI 撤下，其后台只读查询保留。
- 作业详情改为独立主区域页面；今日 Lab 0 直接跳到作业页，说明与两份附件正常显示，教师无需提交规则仍生效。未选择或发送学校提交文件。
- 浏览器预览验证真实课程进入/播放/后退、今日到作业详情、刷新恢复详情、返回列表；页面无弹窗遮罩，侧栏保持可用，控制台无警告或错误。22 项前端测试通过，覆盖历史学期返回筛选、作业详情链接与提交不自动触发；类型、构建、格式和 token 检查通过。
- 已安装本机 0.6.0 更新，安装二进制与 release SHA-256 一致（6d3c0df200759103b2f290b508c5e1e74f940ccd014a44c6d01b031c9b5583c0）。本轮原生安装后的深入交互检查受用户操作与屏幕捕获错误影响，主区域详情旅程以独立预览的真实数据验证为依据；未把它计为新的原生播放验收。

## 本机课程资料与界面精简（2026-09-09）

- 增加系统文件多选导入，复用现有课程归档目录；复制原件，同内容复用，同名异内容保留版本。通过不含路径的课程/账号范围文件 ID 打开与删除，拒绝失效 ID、其他课程文件和符号链接。删除使用 macOS NSFileManager 废纸篓 API，可从废纸篓拖出恢复；保留来源记录。
- 本机资料列表同时显示个人导入与已下载教学网文件，提供打开、文件夹入口和行内确认删除；教学网读取与批量下载继续保留。取消添加、部分失败、删除失败重试由前端回归测试覆盖。
- 提醒页、原生通知插件/权限与后台检查已移除；旧提醒地址回到今日。无截止时间列表留空，详情用破折号。
- 核心 33 项、前端 24 项测试通过，格式与 token 校验通过；桌面构建成功，安装二进制 SHA-256 与产物一致：759a348477a0eafea5f652251779b8523059f5c63627b2e721d16583405de9be。
- 原生实际看到新资料分区、添加按钮、精简后的今日作业与导航。用户在使用应用并切换课程时停止自动操作，未完成系统选择器到打开/废纸篓的完整手动验收；未删除任何个人资料。复制、内容去重与版本保护已用临时文件实际验证。

## 成绩页收敛（2026-09-10）

- 按用户决定移除成绩页的教学网成绩页签、选课器和 allCourses 请求，仅保留树洞 scores 数据与学期筛选；课程详情的教师过程分数和作业反馈保持原入口。
- 原生 app 构建通过并安装，正在运行的进程未强制重启，下次启动生效。
- 真实后端浏览器验收读取 19 条成绩；切换 25-26 第 3 学期正确只显示听觉文化与世界文明，截图确认布局正常。缺失 GPA 显示破折号，未自行计算。
- 培养方案调研：教务部 2026 届毕业审查通知确认入口“校内门户 → 教务部业务 → 毕业审查”（https://dean.pku.edu.cn/web/notice_details.php?id=724）；城环教务办 FAQ 说明四年学习期间可查看，系统匹配完成学分（含当前学期选课）和个人教学计划（https://ues.pku.edu.cn/jyjx/bksjy/jxgl/xyjwbyw/index.htm）。
- 未发现公开的完成度 API 或现有 PKU CLI 接入。elective_query 的 education_plan_bk 是可选课程分类，不是个人完成度。用户完成 IAAA 登录后，真实查看主修毕业审查；JSON 状态接口 success=true、hasjxjh=false，页面提示“还未分配教学计划，请联系院系教务员。”，当前账号尚无完成度可读。未修改任何课程匹配或提交审查。

## 课程按学期展开与成绩去重（2026-09-10）

- 课程列表撤掉学期选择器，按源学期降序分组全部展开，未知学期置后；搜索跨学期，详情返回保留搜索。
- 成绩去掉重复的课程成绩标题、学期标签和常驻来源说明；筛选学期与刷新位于一行，指定学期时隐藏表格重复学期列。
- 27 项前端测试通过，类型检查及原生构建通过。真实后端看到 29 门课分为 4 个学期（8/1/6/14），搜索人工智能跨学期显示三门课程；成绩 19 条，筛选第 3 学期只显示 96 分课程。两页截图及控制台检查通过。已安装并重开新版。
- 通知扩展仅完成调研，尚未新增订阅：门户各单位公告 100 条真实样本包括几十个院系部门，优先考虑按发布单位订阅；智能学院官网通知与学术交流、计算中心官网、讲堂活动为可选补充。智能学院通知偏研究生，讲堂活动列表分音乐/舞/剧/戏/电影/讲座/展览，不能只采音乐页后声称全部活动。

## 2026-09-10 回放字幕

- 原生安装包构建通过；37 项核心测试、29 项前端测试通过（包含 SRT/VTT 解析、时间轴边界、内容签名隔离、重载、子进程取消、原生文本轨道与前端生成/取消状态）。
- 使用真实《计算机系统导论》2026-09-07 第 5-6 节回放，视频时长 7166.52 秒，提取音频 7166.72 秒。整节经 Belle Whisper 中文模型、Silero 人声检测和词级时间戳生成 1344 条字幕，单条最长 38 字。正常讲课片段能识别课程内容；先前小模型在 59 分附近课间的重复误识别经过人声过滤后消失。没有进行逐字人工准确率评测，课程术语仍有错误。
- IAB 普通播放和原生全屏均实际显示字幕；桌面 WKWebView 也显示原生字幕。原生 OS 文件选择器导入整节真实 SRT 成功，文件来源名更新，字幕自动装载；字幕关闭后消失，打开后恢复。
- 实际点击桌面「重新生成」，关闭播放器再打开，后台已从准备音频进入识别；点击取消后显示原有字幕保留。磁盘上的 1344 条 Belle 字幕未变化，临时任务目录已清理。
- macOS 的扩展名过滤会把未注册类型的 SRT 置灰，已取消 OS 层过滤，由核心按扩展名、UTF-8、大小和时间轴检查；没有扩大网页 IPC 文件路径权限。
- 本机 Python 与模型独立于 app，适配配置见 SUBTITLES.md。字幕现已按教学网稳定账号保存，同账号重新登录后自动复用，当前会话旧字幕自动迁移。分段版本退出应用保留已完成片段，重开后可继续生成。

字幕账号持久化修复：真实教学网 `/learn/api/public/v1/users/me` 返回稳定账号标识，两个独立认证客户端一致。当前已生成的 Belle 中文 1,344 条字幕自动迁移后逐字节一致，真实回放 Prepare → SubtitleStatus 直接加载全部字幕。38 项核心测试通过，覆盖凭证世代变化后的同账号字幕读取、不同账号隔离、旧副本不覆盖新字幕、回放内容变化拒绝错配。本轮未操作用户退出或扫码重新登录，凭证轮换通过隔离测试验证。

## 2026-09-10 分段字幕

- 真实《计算机系统导论》回放复用已缓存视频，约 37 秒时保存了前 7 分钟字幕；未等整节完成即可在视频 5:05 位置看到本轮新增字幕，控制台无错误。首条新字幕 286.74 秒，与此前整节识别的人声开始时间一致；每次提取音频约 62 秒，含边界上下文。该耗时不代表未缓存或弱网下载速度。
- 界面停止时保存前 11 分钟、81 条新字幕；退出并重启后端进程，再打开仍显示「继续生成」和 11 分钟进度。继续实际从第 11 分钟下载/识别，推进到第 16 分钟后停止，原始 1,344 条完整字幕文件逐字节未改动。已有分段字幕与原轨道剩余部分可继续观看。
- 39 项核心测试、30 项前端测试通过；覆盖只读取所需 HLS 分片、无声片段持久化、重开续跑、边界上下文裁剪、原轨道剩余部分保留、播放器动态追加字幕、空字幕前缀的继续入口。构建、类型检查、格式和 token 检查通过。
- 安装后的原生应用实际打开同一回放，显示新增字幕和「已保存前 16 分 0 秒」「继续生成」。播放器暂停并恢复到原先 6:05 位置；验收后台已停止，无活动识别任务。安装包与构建二进制 SHA-256 一致。

## 2026-09-10 字幕信息组织

- 字幕面板默认仅显示开关、简短进度与生成/继续/停止操作。导入、延迟、来源、重新生成位于「更多」；「字幕模型设置」跳转并聚焦设置页对应区域。真实回放有 16 分钟断点时默认可见「显示字幕 / 已生成 16:00 / 继续生成 / 更多」，无重复说明。
- 「设置 → 字幕」实际列出 Belle 中文、Whisper Small、Whisper Tiny。真实选择 Tiny，刷新设置页后选择持久保留；返回回放变为「重新生成」，避免接续不同模型的旧断点。已恢复 Belle 中文；本轮模型切换验收未启动识别任务。原生应用中用户已启动的其他识别任务继续运行。
- 浏览器截图确认字幕主面板、更多工具与模型设置分别呈现；页面无运行时错误。40 项核心测试、30 项前端测试通过；新增模型选择校验完整本地权重、拒绝路径/未知模型 ID、保留 Python/FFmpeg 运行配置。现有交互测试确认默认隐藏低频工具，展开后仍可使用。
- 新版 app 已覆盖安装且与构建二进制 SHA-256 一致。由于原生应用中仍有用户的识别任务，未退出或重启该进程；新 UI 在下次重开应用生效。开发预览已停止、验收页已关闭。

## 本科 GPA 与加权平均分（2026-09-10）

- 已核对教务部《北京大学本科生成绩评定和记载办法（2019 年 9 月修订）》第十三条：<https://dean.pku.edu.cn/web/rules_info.php?id=173>。逐条考核按连续公式转换绩点、按学分加权，保留重修记录，最终显示时保留两位小数。
- 学校返回有效 GPA 时优先显示；缺失时本地计算并标注「按官方规则计算」。学分加权平均分使用相同纳入范围，单独标注「本地计算」。累计已获学分始终使用学校返回值。
- 非百分制状态、无效分数或学分、明确标注的毕业论文和综合性考试排除。源数据没有完整 GPA 资格标记，后两类依课程名称及类别识别；此边界、计入条数和学分在 GPA 旁问号的简短浮层中披露，公式通过官方规则入口查看。
- 真实账户的当前成绩记录在本地浏览器预览验证：全部学期的统计和第二学期切换均与独立 Decimal 计算一致，表格筛选与指标同步，说明以问号入口默认收起；没有修改学校记录。真实成绩值不写入测试夹具。
- 前端 37 项测试通过（其中新增 7 项成绩计算及页面测试），包含小数成绩、学分加权、0 分、不及格、重修、非百分制排除、空数据、无效数据、学校 GPA 优先与学期筛选。TypeScript、生产构建及设计 token 检查通过。
- 原生安装包更新时保留用户正在运行的字幕任务，不退出应用；新页面在下次重新打开应用后生效。当前运行中的旧进程未作为新页面的原生验收证据。

## 门户通知复用阅读窗口（2026-09-10）

- 用户指出校工会通知打开时要求验证，且每次原文窗口需等待数秒。当前条目确认为校内门户的各单位公告；未将此前对教学网 Cookie 的可能性分析作为本问题结论，也未修改教学网会话处理。
- 旧行为每次为门户通知创建新的 WebView，从门户首页启动整套页面。一次无登录请求耗时约 3.2 秒，最终返回门户认证入口；该结果只说明这次未登录网络路径，不代表所有用户或已登录时的页面耗时。
- 新行为只对 HTTPS 校内门户的标准通知路由使用一个常驻原文窗口。关闭窗口时隐藏，随后打开通知时恢复；已加载门户内切换通知只更新官方的 hash 路由。同一次运行中保留页面、缓存和网页会话，退出应用仍正常结束窗口。
- 正在加载或正常验证时，保留当前页面并记录最后选择的通知；登录完成回到门户后再打开该通知。仍展示学校原网页，未抓取或改写正文，也未自动填写、绕过或替用户完成学校验证。
- 新增 3 项原生路由测试，覆盖精确来源限制、仅在门户文档内更换通知 hash，以及认证页识别。原生 cargo check 和测试通过；首次学校加载和学校会话失效仍可能要求等待或验证。

- 独立原生验收实例（单独的应用标识与网页数据）实际打开同一条校工会公告并进入统一认证；切到扫码页后关闭窗口，主界面恢复焦点，再点另一条门户通知立即恢复原扫码页，未回到初始账号登录表单。关闭和重开复用已加载页面已验证；未要求用户在测试实例登录，因此登录后的通知间切换速度和最新目标在登录完成后的跳转尚未做真实账户验收。
- 测试实例正常退出，用户原生应用及其字幕任务保持运行。最终安装包与构建二进制哈希一致，新行为下次重新打开用户应用生效。
